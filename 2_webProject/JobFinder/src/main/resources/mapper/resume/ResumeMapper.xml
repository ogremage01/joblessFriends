<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.joblessfriend.jobfinder.resume.dao.ResumeDao">

	<select id="findResumesByMemberId" resultType="com.joblessfriend.jobfinder.resume.domain.ResumeVo">
        SELECT 
            RESUME_ID AS resumeId,
            NAME AS name,
            BIRTHDATE AS birthdate,
            PHONENUMBER AS phoneNumber,
            EMAIL AS email,
            ADDRESS AS address,
            SELF_INTRODUCTION AS selfIntroduction,
            MEMBER_ID AS memberId,
            PROFILE AS profile
        	FROM RESUME
        	WHERE MEMBER_ID = #{memberId}
        	ORDER BY RESUME_ID DESC
	</select>

    <delete id="deleteResumeById">
        DELETE FROM RESUME 
        WHERE RESUME_ID = #{resumeId}
        AND MEMBER_ID = #{memberId}
    </delete>

</mapper>